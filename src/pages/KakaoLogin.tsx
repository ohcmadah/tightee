export const kakaoLoader = () => {
  const searchParams = new URLSearchParams(location.search);
  const code = searchParams.get("code");
};

const KakaoLogin = () => {
  return <div></div>;
};

export default KakaoLogin;
